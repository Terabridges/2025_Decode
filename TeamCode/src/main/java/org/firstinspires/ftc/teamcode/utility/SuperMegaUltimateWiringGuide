DRIVE:
    HARDWARE:
    4 drive motors (BR) (BL) (FR) (FL)

    CHUB:

    EUB:

INTAKE:
    HARDWARE:
    spinner motor also transfer spinner (spinner)
    2 axon max servos to lower spinner CR, analog (raiserLeft) (raiserRight)

    CHUB:

    EUB:

SHOOTER:
    HARDWARE:
    turret axon max CR, analog (turret)
    2 shooter motors (flyLeft) (flyRight)
    mini for hood CR, analog (hood)
    magnetic limit switch for turret (turretSwitch)

    CHUB:

    EUB:

TRANSFER:
    HARDWARE:
    spindex motor (spindex)
    swinger into ball max SERVO (clutch)

    CHUB:

    EUB:

VISION:
    HARDWARE:
    color sensor for first ball (ballInspector)
    limelight on shooter (limelight)

    CHUB:

    EUB: