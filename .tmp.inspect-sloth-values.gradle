allprojects {
  tasks.matching { it.name == 'deploySloth' }.configureEach { t ->
    println("TASK=" + t.path)
    println("adbExecutable=" + t.properties['adbExecutable'])
    println("deployLocation=" + t.properties['deployLocation'])
    println("outputDir=" + t.properties['outputDir'])
  }
}
